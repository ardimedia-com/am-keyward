@page "/amkeyward/vaults"
@attribute [Authorize]
@inject NavigationManager Nav

<PageTitle>AM KEYWARD</PageTitle>

@code {
    protected override void OnInitialized() => Nav.NavigateTo(KeywardRoutes.PersonalVaults, replace: true);
}
